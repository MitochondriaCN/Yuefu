# 乐府答辩 PPT — 设计方案

## 一、项目定位

基于 Vue 3 + Vite 的单页演示应用，以 16:9 固定比例呈现，面向线上会议屏幕共享场景。采用明亮优雅的蓝黄绿配色系统（简洁明亮底色 + 蓝紫强调色 + 暖黄辅助色），通过 GSAP 实现流畅的页面切换与内容展开动画。

---

## 二、技术架构

| 层级 | 选型 | 说明 |
|------|------|------|
| 框架 | Vue 3 + Vite | Composition API，响应式数据驱动幻灯片状态 |
| 动画 | GSAP | 页面切换、元素入场、数字滚动、动态背景、SVG 描边 |
| 字体 | Google Fonts CDN | Space Grotesk（展示标题）、Lora（编辑衬线）、Inter（正文） |
| 图标 | lucide-vue-next | 全项目统一图标源 |
| 样式 | CSS Variables | 严格映射 DESIGN.md 色彩系统 |
| 翻页 | Keyboard Events | 监听 ArrowLeft / ArrowRight / Space |

---

## 三、视觉规范

### 3.1 色彩映射

```css
:root {
  --light-bg: #FCFBFA;          /* 全局背景：简洁明亮的底色 */
  --bg-off-white: #FFFFFF;      /* 次级表面、卡片背景 */
  --text-primary: #2D2D2D;      /* 主标题、正文 */
  --text-secondary: #73715C;    /* 副标题、描述（墨绿灰） */
  --text-muted: #999999;        /* 辅助文字 */
  --blue-accent: #5266E8;       /* 强调色：蓝紫，用于标题、CTA、活跃状态 */
  --yellow-main: #DDD575;       /* 辅助色：暖黄，用于装饰、高亮、标签 */
  --yellow-light: #DBD37F;      /* 浅黄：背景点缀、图标底色 */
  --green-dark: #73715C;        /* 墨绿：副标题、装饰文字 */
  --green-muted: #B8BA99;       /* 橄榄绿：边框、分割线 */
  --border-sand: rgba(184, 186, 153, 0.3);  /* 卡片边框（带透明度） */
  --surface-light: rgba(184, 186, 153, 0.1); /* 次级按钮背景 */
}
```

### 3.2 字体层级

| 角色 | 字体 | 尺寸 | 字重 | 行高 | 字距 |
|------|------|------|------|------|------|
| 封面主标题 | SF Pro Display, PingFang SC | 96px | 800 | 0.95 | -2px |
| 页面标题 | SF Pro Display, PingFang SC | 48px | 500 | 1.0 | normal |
| 英文副标题 | serif（衬线体） | 48px | 400 | 1.08 | 2px |
| 正文大 | SF Pro Display, PingFang SC, Helvetica Neue | 20px | 400 | 1.6 | normal |
| 正文 | SF Pro Display, PingFang SC, Helvetica Neue | 16px | 400 | 1.5 | normal |
| 标签 | SF Pro Display, PingFang SC | 14px | 600 | normal | 0.5px（uppercase） |
| 数据展示 | SF Pro Display, PingFang SC | 64px | 800 | 1.0 | normal |
| Slogan | SF Pro Display, PingFang SC | 26px | 300 | 1.3 | 6px |

### 3.3 组件规范

**卡片**
- Background: `rgba(255, 255, 255, 0.7)`
- Border: `1px solid rgba(184, 186, 153, 0.3)`
- Border Radius: `24px`
- Shadow: `0 15px 35px rgba(115, 113, 92, 0.05)`（浅色投影增加层次）
- Hover: `transform: translateY(-10px)`, background `#FFFFFF`

**主按钮（Primary Blue）**
- Background: `#5266E8`
- Text: `#FFFFFF`
- Padding: `8px 16px`
- Border Radius: `4px`
- Border: `1px solid #5266E8`

**次级按钮（Primary Dark）**
- Background: `#73715C`
- Text: `#FFFFFF`
- Padding: `20px 24px`
- Border Radius: `8px`
- Hover: background `#B8BA99`, text `#73715C`

**标签（Caption Upper）**
- `text-transform: uppercase`
- Letter Spacing: `0.5px`
- Font: SF Pro Display / PingFang SC 14px weight 600

**团队标签（Team Tag）**
- Background: `#DDD575`
- Text: `#73715C`
- Padding: `5px 15px`
- Border Radius: `20px`
- Font: 14px weight bold

**结论横幅（Conclusion Banner）**
- Background: `linear-gradient(90deg, #73715C, #4A493B)`
- Text: `#FFFFFF`
- Border Radius: `100px`
- Padding: `15px 40px`

## 重点要求
- 禁止使用任何 emoji
- 多使用图标，增加美观度，图标库lucide

---

## 四、页面结构（共 11 页）

| 页码 | 页面名称 | 内容概要 | 展开策略 |
|------|----------|----------|----------|
| 1 | **封面** | 项目名称、英文副标题、团队信息 | 标题 → 副标题 → 团队，依次淡入 |
| 2 | **项目背景·上** | 立项原因、需求判断（即拍即听） | 标题 → 正文段落 → 关键词高亮 |
| 3 | **项目背景·下** | 64份问卷数据卡片、用户画像 | 标题 → 3个数据卡片依次飞入，数字 count-up |
| 4 | **四大痛点** | 识谱门槛、心理预演、反馈周期、工具碎片 | 标题 → 4卡片网格，依次缩放入场 |
| 5 | **竞品分析** | 四类竞品 + 空白矩阵 | 标题 → 竞品类别 → 核心空白结论 |
| 6 | **核心功能** | 五大功能卡片 + 双模型说明 | 标题 → 5卡片依次从底部滑入 |
| 7 | **技术架构** | 三层架构图（客户端/后端/云函数） | 标题 → 三层堆叠，逐层展开 |
| 8 | **Legato OMR 模型** | 模型架构图 + 关键数据 | 标题 → 架构图绘制动画 → 数据点 |
| 9 | **社会价值** | 无障碍应用 + 文化遗产数字化 | 标题 → 两大价值卡片，左右分栏 |
| 10 | **演示视频占位** | 播放按钮占位区、提示文字 | 整体淡入 |
| 11 | **结束页** | 感谢语、核心 slogan、未来展望 | 标题 → slogan → 展望，配合动态背景 |

---

## 五、动画策略

### 5.1 全局页面切换

- **效果**：当前页内容向左侧滑出（`x: 0 → -100px`, `opacity: 1 → 0`），新页内容从右侧滑入（`x: 100px → 0`, `opacity: 0 → 1`）
- **时长**：500ms
- **缓动**：`power2.inOut`
- **实现**：父容器 `<SlideContainer>` 维护 `currentPage` 状态，通过 GSAP 时间轴控制离场/入场

### 5.2 内容展开机制

每页组件维护一个 `stepIndex` 响应式状态。按键盘 **右方向键 / 空格** 时：

1. 若 `stepIndex < maxSteps`：`stepIndex++`，触发下一批元素的入场动画
2. 若 `stepIndex === maxSteps`：执行页面切换动画，进入下一页

按 **左方向键** 时：
- 若 `stepIndex > 0`：`stepIndex--`，当前页元素反向退场
- 若 `stepIndex === 0`：返回上一页（上一页恢复至最后一步状态）

### 5.3 元素入场动画库

| 动画名 | 效果 | 时长 | 缓动 | 适用场景 |
|--------|------|------|------|----------|
| `fadeUp` | `y: 30px → 0`, `opacity: 0 → 1` | 600ms | `power3.out` | 标题、正文段落 |
| `fadeScale` | `scale: 0.9 → 1`, `opacity: 0 → 1` | 500ms | `back.out(1.2)` | 卡片、数据块 |
| `slideFromRight` | `x: 60px → 0`, `opacity: 0 → 1` | 500ms | `power2.out` | 列表项、功能点 |
| `countUp` | 数字从 0 滚动至目标值 | 1200ms | `power2.out` | 数据卡片 |
| `drawLine` | SVG `stroke-dashoffset` 从 100% 到 0 | 800ms | `power2.inOut` | 架构图连接线 |

### 5.4 动态背景（封面 & 结束页）

**效果**：蓝紫（`#5266E8`）与暖黄（`#DDD575`）的柔和弥散渐变，配合抽象几何色块装饰，营造明亮优雅的视觉氛围。

**技术实现**：
- 使用绝对定位的模糊色块（`filter: blur(60px)`）作为装饰
- 右上角：400px 蓝紫色圆（`#5266E8`，opacity 0.15）
- 左下角：500px 暖黄色圆（`#DDD575`，opacity 0.15）
- 封面叠加一层半透明白色遮罩（`rgba(255, 254, 251, 0.75)`）确保文字可读性
- 可选：使用 Canvas 2D 绘制多层缓慢漂移的径向渐变球体

**五线谱装饰线条**：
- 宽度：150%，高度：2px
- 背景：`linear-gradient(90deg, transparent, #DBD37F, transparent)`
- 旋转：`-10deg`
- 动画：15s 线性循环平移（`translateX(-30%) → translateX(10%)`）
- 透明度：0.4

**配色参数**：
| 层次 | 颜色 | 尺寸 | 漂移速度 | 透明度 |
|------|------|------|----------|--------|
| 底层 | `#5266E8` | 800px | 最慢 | 0.15 |
| 中层 | `#DBD37F` | 600px | 中等 | 0.12 |
| 上层 | `#DDD575` | 500px | 较快 | 0.10 |

---

## 六、交互设计

### 6.1 键盘控制

| 按键 | 行为 |
|------|------|
| `ArrowRight` / `Space` | 下一步（元素展开或翻页） |
| `ArrowLeft` | 上一步（元素回退或返回上一页） |
| `Home` | 跳转到第一页 |
| `End` | 跳转到最后一页 |

### 6.2 页面状态管理

```
App
├── currentPage: number        // 当前页码（1-based）
├── isAnimating: boolean       // 是否处于动画中（防抖）
└── SlideRenderer
    ├── pages: Slide[]         // 11 个页面组件列表
    ├── pageHistory: number[]  // 浏览历史（用于返回）
    └── perPageState: Map<page, stepIndex>  // 每页的展开进度
```

### 6.3 防抖机制

页面切换和内容展开期间设置 `isAnimating = true`，屏蔽键盘输入，防止快速按键导致动画堆叠或状态错乱。动画完成后释放。

---

## 七、文件目录结构

```
slidev/                     # 项目根目录
├── public/
│   └── images/             # 素材占位目录（未来放置截图、架构图等）
├── src/
│   ├── assets/
│   │   └── styles/
│   │       ├── tokens.css      # CSS 变量（色彩、字体、间距）
│   │       ├── base.css        # 全局 reset + 字体加载
│   │       └── animations.css  # 通用动画关键帧
│   ├── components/
│   │   ├── ui/                 # 原子级 UI 组件
│   │   │   ├── Card.vue
│   │   │   ├── Button.vue
│   │   │   ├── Tag.vue
│   │   │   └── CountUp.vue
│   │   ├── layout/
│   │   │   ├── SlideContainer.vue    # 幻灯片外层容器（16:9 比例）
│   │   │   ├── SlideTransition.vue   # 页面切换动画包装器
│   │   │   └── FluidBackground.vue   # 封面/结束页动态背景
│   │   └── content/
│   │       ├── DataCard.vue          # 数据展示卡片（带 count-up）
│   │       ├── PainPointCard.vue     # 痛点卡片
│   │       ├── FeatureCard.vue       # 功能卡片
│   │       ├── ArchitectureLayer.vue # 架构层级块
│   │       └── CompetitorMatrix.vue  # 竞品对比矩阵
│   ├── slides/                 # 每一页幻灯片组件
│   │   ├── Slide01_Cover.vue
│   │   ├── Slide02_BackgroundA.vue
│   │   ├── Slide03_BackgroundB.vue
│   │   ├── Slide04_PainPoints.vue
│   │   ├── Slide05_Competitor.vue
│   │   ├── Slide06_Features.vue
│   │   ├── Slide07_Architecture.vue
│   │   ├── Slide08_Legato.vue
│   │   ├── Slide09_SocialValue.vue
│   │   ├── Slide10_VideoPlaceholder.vue
│   │   └── Slide11_Ending.vue
│   ├── composables/
│   │   ├── useSlideNavigation.ts     # 键盘导航 + 页面状态管理
│   │   ├── useStepReveal.ts          # 单页内 step-by-step 展开逻辑
│   │   └── useCountUp.ts             # 数字滚动动画逻辑
│   ├── App.vue
│   └── main.ts
├── index.html
├── package.json
├── vite.config.ts
└── tsconfig.json
```

---

## 八、开发计划

| 阶段 | 任务 | 预计产出 |
|------|------|----------|
| Phase 1 | 搭建项目骨架 | Vite + Vue 3 项目初始化，安装 GSAP / lucide-vue-next，配置 Google Fonts |
| Phase 2 | 设计系统落地 | tokens.css、base.css、动画关键帧，Button / Card / Tag 基础组件 |
| Phase 3 | 核心机制实现 | useSlideNavigation、useStepReveal、SlideContainer、SlideTransition |
| Phase 4 | 动态背景 | FluidBackground 组件（Canvas 流体渐变） |
| Phase 5 | 逐页实现 | 按页码顺序实现 11 个 Slide 组件，每页包含具体的内容排版和展开动画 |
| Phase 6 | 联调与优化 | 键盘导航流畅度、动画时序微调、边界情况处理 |

---

## 九、每页设计文档索引

以下页面将在后续单独编写详细设计文档：

- [ ] `Slide01_Cover.md` — 封面页
- [ ] `Slide02_BackgroundA.md` — 项目背景（上）
- [ ] `Slide03_BackgroundB.md` — 项目背景（下）
- [ ] `Slide04_PainPoints.md` — 四大痛点
- [ ] `Slide05_Competitor.md` — 竞品分析
- [ ] `Slide06_Features.md` — 核心功能
- [ ] `Slide07_Architecture.md` — 技术架构
- [ ] `Slide08_Legato.md` — Legato OMR 模型
- [ ] `Slide09_SocialValue.md` — 社会价值
- [ ] `Slide10_VideoPlaceholder.md` — 演示视频占位
- [ ] `Slide11_Ending.md` — 结束页

---

*文档版本：1.0.0*
*最后更新：2026-04-23*
