# 乐府（Yuefu）项目文档

## 一、项目概述

**乐府（Yuefu）** 是一款面向音乐学习者的 AI 乐谱识别与预览 Android 应用。用户可通过拍照或导入图片，利用 AI 进行五线谱光学音乐识别（OMR），将识别结果转化为可试听、可视化的钢琴瀑布流演奏效果。同时支持基于提示词的 AI 实时乐曲编配。

- **产品定位**：基于 AI 五线谱识别的乐谱即时预览与理解系统
- **目标用户**：音乐初学者、需要快速预览乐谱效果的演奏者、扒谱效率追求者
- **架构模式**：C/S 架构，前端负责 XML 解析与音频合成，后端负责 OMR 识别

---

## 二、技术栈

| 层级 | 技术选型 | 版本 |
|------|----------|------|
| 语言 | Kotlin | 2.2.21 |
| UI 框架 | Jetpack Compose + Material 3 | BOM 2025.12.00 |
| 依赖注入 | Hilt (Dagger) | 2.57.2 |
| 本地数据库 | Room | 2.8.4 |
| 网络请求 | Retrofit + OkHttp + Kotlinx Serialization | 3.0.0 |
| 图片加载 | Coil | 2.7.0 |
| XML 解析 | Dom4j + Jaxen | 2.2.0 |
| 本地存储 | DataStore Preferences | 1.2.0 |
| MIDI 驱动 | MidiDriver (本地 AAR) | 1.29 |
| 构建工具 | Gradle + KSP | AGP 8.13.2 |

---

## 三、项目结构

```
Yuefu/
├── app/
│   ├── src/main/java/com/xianliticn/yuefu/
│   │   ├── MainActivity.kt              # 主入口，底部导航框架
│   │   ├── SheetActivity.kt             # 乐谱详情页面（独立 Activity）
│   │   ├── YuefuApplication.kt          # Application 类，全局异常处理
│   │   ├── AppDatabase.kt               # Room 数据库定义
│   │   ├── NavItem.kt                   # 导航项数据类
│   │   ├── daos/
│   │   │   └── SheetDao.kt              # 乐谱数据访问对象
│   │   ├── entities/
│   │   │   └── Sheet.kt                 # 乐谱实体（Room 实体）
│   │   ├── modules/                     # Hilt DI 模块
│   │   │   ├── DatabaseModule.kt        # 数据库注入
│   │   │   ├── NetworkModule.kt         # 网络客户端注入
│   │   │   ├── SettingsModule.kt        # 设置管理器注入
│   │   │   └── SettingsManager.kt       # DataStore 设置管理
│   │   ├── music/                       # 音乐核心引擎
│   │   │   ├── Parser.kt                # MusicXML 解析器
│   │   │   ├── SequenceEngine.kt        # MIDI 序列播放引擎
│   │   │   ├── MidiEvent.kt             # MIDI 事件定义
│   │   │   ├── VisualNoteEvent.kt       # 可视化音符事件
│   │   │   └── Note.kt                  # 音符类型枚举
│   │   ├── pages/                       # 页面模块（按功能分目录）
│   │   │   ├── home/                    # 主页
│   │   │   ├── sheet/                   # 乐谱列表页
│   │   │   ├── scanstudio/              # 扫描工作室（OMR）
│   │   │   ├── sheetoverview/           # 乐谱概览
│   │   │   ├── sheetplay/               # 乐谱演奏（钢琴瀑布流）
│   │   │   ├── composition/             # AI 编曲（Lyria）
│   │   │   ├── settings/                # 设置页
│   │   │   └── about/                   # 关于页
│   │   ├── ui/components/               # 可复用 UI 组件
│   │   │   ├── NoteFlow.kt              # 音符下落 Canvas 组件
│   │   │   ├── PianoRollNoteFlow.kt     # 钢琴键盘 + 瀑布流组件
│   │   │   ├── MessageBubble.kt         # 聊天气泡组件
│   │   │   ├── ChoiceGroup.kt           # 选项组组件
│   │   │   ├── InfoBox.kt               # 信息框组件
│   │   │   ├── InputDialog.kt           # 输入对话框
│   │   │   └── ScanningTutorialBottomSheet.kt  # 扫描教程底部弹窗
│   │   ├── ui/theme/                    # 主题配置
│   │   │   ├── Color.kt                 # 颜色定义
│   │   │   ├── Theme.kt                 # 主题方案
│   │   │   └── Type.kt                  # 字体排版
│   │   ├── utils/                       # 工具类
│   │   │   ├── FileUtils.kt             # 文件路径与哈希
│   │   │   ├── MusicXmlUtils.kt         # MusicXML 读写与提取
│   │   │   ├── ImageUtils.kt            # 图片滤镜变换
│   │   │   ├── DateTimeUtils.kt         # 时间格式化
│   │   │   └── ColorUtils.kt            # 颜色工具
│   │   ├── vo/                          # 值对象 / 网络响应模型
│   │   │   ├── ApiResponse.kt           # 通用 API 响应包装
│   │   │   ├── SubmitTaskVo.kt          # OMR 任务提交响应
│   │   │   ├── TaskStatus.kt            # 任务状态枚举
│   │   │   └── SystemInfoVo.kt          # 系统信息响应
│   │   └── webapi/                      # 网络 API 接口
│   │       ├── SystemInfoApi.kt         # 系统信息 API
│   │       ├── SurveyApi.kt             # 问卷 API（已废弃）
│   │       ├── omr/                     # OMR 相关 API
│   │       │   ├── OmrApi.kt            # OMR 接口定义
│   │       │   └── OmrEngine.kt         # OMR 引擎枚举
│   │       └── lyria/                   # Lyria AI 编曲 API
│   │           ├── LyriaResponse.kt     # Lyria 响应模型
│   │           ├── PromptMessage.kt     # 提示词消息
│   │           ├── PlaybackMessage.kt   # 播放控制消息
│   │           ├── ConfigMessage.kt     # 配置消息
│   │           ├── MusicGenerationConfig.kt  # 音乐生成配置
│   │           ├── Scale.kt             # 调式枚举
│   │           ├── PlaybackControl.kt   # 播放控制枚举
│   │           ├── ClientContent.kt     # 客户端内容
│   │           └── WeightedPrompt.kt    # 加权提示词
│   └── src/main/res/                    # Android 资源文件
├── build.gradle.kts                     # 根构建脚本
├── app/build.gradle.kts                 # App 模块构建脚本
├── gradle/libs.versions.toml            # 依赖版本管理
└── settings.gradle.kts                  # Gradle 设置
```

---

## 四、核心模块详解

### 4.1 导航架构

应用采用双 Activity 结构：
- **MainActivity**：主框架，底部导航包含四个 Tab（主页/乐谱/创作/设置）
- **SheetActivity**：乐谱详情页，底部导航包含两个 Tab（概览/演奏），支持横屏演奏模式

导航使用 Jetpack Compose Navigation，通过 `rememberNavController` 管理路由栈。

### 4.2 数据层

#### Room 数据库
- **实体**：`Sheet` — 存储乐谱元数据（任务ID、下载状态、文件名、乐谱名、创建时间、最后打开时间、文件哈希、所用模型）
- **DAO**：`SheetDao` — 支持查询全部、已下载/未下载、按文件名/乐谱名模糊搜索、按哈希查重
- **版本**：当前数据库版本 2，支持 AutoMigration

#### DataStore
- `SettingsManager` 管理用户偏好设置（如是否显示扫描教程）

### 4.3 音乐引擎

#### Parser（MusicXML 解析器）
- 将 MusicXML 文档解析为 MIDI 事件列表
- 支持：多声部（Part）、拍号/速度变化、和弦（Chord）、连音（Tie）、休止符
- 支持自定义 BPM 覆盖
- 将 MIDI 事件进一步转换为可视化音符事件（`VisualNoteEvent`），用于钢琴瀑布流渲染

#### SequenceEngine（MIDI 序列引擎）
- 基于 `MidiDriver` 的实时 MIDI 播放
- 支持播放/暂停/停止/进度跳转
- 使用协程 + 忙等待（Busy Wait）保证微秒级定时精度
- 进度流：`currentProgressMillis`（StateFlow），以约 31Hz 频率更新

#### 钢琴瀑布流可视化
- `NoteFlow`：Canvas 绘制的音符下落组件，支持视口剔除优化
- `PianoRollNoteFlow`：集成 88 键钢琴键盘 + 瀑布流，支持滚动模式和滑奏触摸
- 钢琴键盘支持白键/黑键层叠布局、按下状态反馈、渐变光照效果

### 4.4 OMR 流程

```
用户拍照/选图 → 图片预处理（亮度/对比度/饱和度/裁剪） → 提交到后端 OMR → 
轮询任务状态 → 下载 MusicXML → 解压/验证/查重 → 存入本地数据库
```

- **OMR 引擎**：支持两种模型
  - `QINGSHANG（清商）` — FP16 快速模型
  - `ZHENGSHENG（正声）` — FP32 高精度模型
- 图片预处理使用自定义 Coil `ColorFilterTransformation`
- 支持 `.mxl` 压缩格式自动解压
- 使用 SHA-256 哈希进行重复文件检测

### 4.5 AI 编曲（Lyria）

- 通过 WebSocket 连接后端 Lyria 服务
- 支持发送提示词（Prompt）实时生成音乐
- 音频数据以 Base64 PCM 流形式接收，通过 `AudioTrack` 实时播放
- 支持播放控制（PLAY/PAUSE/STOP/RESET_CONTEXT）
- 支持配置调式（Scale）和 BPM
- UI 采用聊天式交互界面

### 4.6 网络层

- **Base URL**：`https://8.148.249.149`
- **WebSocket**：`wss://8.148.249.149/ws/lyria`
- OkHttpClient 配置了全信任证书（开发环境）和 120 秒读超时
- Retrofit 使用 Kotlinx Serialization 作为转换器

---

## 五、关键依赖说明

| 依赖 | 用途 |
|------|------|
| Jetpack Compose | 声明式 UI |
| Material 3 | 组件库和主题 |
| Hilt | 依赖注入 |
| Room | 本地数据库 |
| Retrofit + OkHttp | HTTP 网络请求 |
| Kotlinx Serialization | JSON 序列化 |
| Coil | 图片加载 |
| Dom4j + Jaxen | MusicXML 解析（XPath）|
| MidiDriver | Android MIDI 软合成器 |
| DataStore | 键值对偏好存储 |

---

## 六、资源文件

- **应用图标**：多密度 mipmap 资源 + 自适应图标
- **字体**：Noto Serif SC Bold（中文字体）
- **图片资源**：开发者头像（webp）、乐谱占位图、扫描教程图
- **字符串**：支持中英文（`values` / `values-en`）
- **配置 XML**：网络安全配置、文件 Provider 路径、备份规则

---

## 七、构建配置

- **compileSdk**：36
- **minSdk**：28（Android 9.0）
- **targetSdk**：36
- **versionCode**：3
- **versionName**：v0.2.10-beta
- **Java/Kotlin Target**：JVM 11
- **代码压缩**：未启用（`isMinifyEnabled = false`）

---

## 八、权限声明

- `READ_EXTERNAL_STORAGE` / `READ_MEDIA_IMAGES` — 读取图片
- `WRITE_EXTERNAL_STORAGE` — 写入外部存储
- `INTERNET` — 网络访问
- MIDI 功能为必需硬件特性

---

## 九、项目背景

- **赛事**：第十九届全国大学生软件创新大赛参赛项目
- **开发团队**：希望之峰小队
- **技术架构**：C/S 架构，Android + Jetpack Compose 前端，Spring Boot + Legato OMR 后端
- **OMR 部署**：基于 Legato 架构魔改，部署于阿里云函数计算（FC）

---

## 十、文件清单（主要源码）

### 入口与配置
- `MainActivity.kt` — 主 Activity，底部导航路由
- `SheetActivity.kt` — 乐谱详情 Activity，支持横屏演奏
- `YuefuApplication.kt` — 全局异常捕获（崩溃日志复制到剪贴板）
- `AppDatabase.kt` — Room 数据库
- `NavItem.kt` — 导航项数据类

### DI 模块
- `modules/DatabaseModule.kt`
- `modules/NetworkModule.kt`
- `modules/SettingsModule.kt`
- `modules/SettingsManager.kt`

### 音乐核心
- `music/Parser.kt` — MusicXML → MIDI → 可视化事件
- `music/SequenceEngine.kt` — MIDI 播放引擎
- `music/MidiEvent.kt`
- `music/VisualNoteEvent.kt`
- `music/Note.kt`

### 页面（Page + ViewModel）
| 页面 | Page | ViewModel |
|------|------|-----------|
| 主页 | `home/HomePage.kt` | `home/HomePageViewModel.kt` |
| 乐谱列表 | `sheet/SheetPage.kt` | `sheet/SheetPageViewModel.kt` |
| 扫描工作室 | `scanstudio/ScanStudioPage.kt` | `scanstudio/ScanStudioPageViewModel.kt` |
| 乐谱概览 | `sheetoverview/SheetOverviewPage.kt` | `sheetoverview/SheetOverviewPageViewModel.kt` |
| 乐谱演奏 | `sheetplay/SheetPlayPage.kt` | `sheetplay/SheetPlayPageViewModel.kt` |
| AI 编曲 | `composition/CompositionPage.kt` | `composition/CompositionPageViewModel.kt` |
| 设置 | `settings/SettingsPage.kt` | `settings/SettingsPageViewModel.kt` |
| 关于 | `about/AboutPage.kt` | `about/AboutPageViewModel.kt` |

### UI 组件
- `ui/components/NoteFlow.kt` — 音符下落 Canvas
- `ui/components/PianoRollNoteFlow.kt` — 钢琴键盘 + 瀑布流
- `ui/components/MessageBubble.kt` — 聊天气泡
- `ui/components/ChoiceGroup.kt`
- `ui/components/InfoBox.kt`
- `ui/components/InputDialog.kt`
- `ui/components/ScanningTutorialBottomSheet.kt`

### 工具类
- `utils/FileUtils.kt` — 文件路径、SHA-256 哈希
- `utils/MusicXmlUtils.kt` — MusicXML 验证、标题/作者/小节数提取
- `utils/ImageUtils.kt` — 亮度/对比度/饱和度变换
- `utils/DateTimeUtils.kt` — 相对时间格式化
- `utils/ColorUtils.kt` — 随机浅色生成

### 网络 API
- `webapi/omr/OmrApi.kt` — OMR 任务提交/状态查询/下载
- `webapi/omr/OmrEngine.kt` — OMR 引擎枚举
- `webapi/SystemInfoApi.kt` — 系统信息
- `webapi/SurveyApi.kt` — 问卷提交（已废弃）
- `webapi/lyria/*.kt` — Lyria WebSocket 消息模型

### VO / DTO
- `vo/ApiResponse.kt`
- `vo/SubmitTaskVo.kt`
- `vo/TaskStatus.kt`
- `vo/SystemInfoVo.kt`
